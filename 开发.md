
我直接给你写一个完整的应用——**Claude Hub**。

---

## 架构

```
你的电脑（始终运行）
┌─────────────────────────────────────┐
│  Claude Hub Server (:3800)          │
│  ┌───────────┐  ┌────────────────┐  │
│  │ Task Queue │→ │ Claude Code    │  │
│  │ & Manager  │  │ (headless -p)  │  │
│  └─────┬─────┘  └───────┬────────┘  │
│        │ WebSocket       │           │
│  ┌─────┴─────────────────┴────────┐  │
│  │    Web Dashboard (SPA)         │  │
│  └────────────────────────────────┘  │
└─────────────────────────────────────┘
        ↑                    ↑
    PC浏览器             手机浏览器
   localhost:3800      192.168.x.x:3800
```

---

## 文件一：`package.json`

```json
{
  "name": "claude-hub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "ws": "^8.18.0"
  }
}
```

## 文件二：`server.js`

```javascript
// ═══════════════════════════════════════════════════════════
//  Claude Hub — Autonomous Task Orchestrator
//  把 Claude Code headless 模式包装成可视化自主开发平台
// ═══════════════════════════════════════════════════════════

const express = require('express');
const { WebSocketServer } = require('ws');
const http = require('http');
const { spawn } = require('child_process');
const path = require('path');
const fs = require('fs');
const crypto = require('crypto');
const os = require('os');

// ── 配置 ─────────────────────────────────────────────────
const CONFIG = {
  port: parseInt(process.env.PORT) || 3800,
  host: '0.0.0.0',
  claudeBin: process.env.CLAUDE_BIN || 'claude',
  maxConcurrent: parseInt(process.env.MAX_CONCURRENT) || 2,
  tasksDir: path.join(__dirname, 'tasks'),
  phaseMaxTurns: [80, 300, 80],  // 规划、开发、报告
};

fs.mkdirSync(CONFIG.tasksDir, { recursive: true });

// ── Express + HTTP + WebSocket ───────────────────────────
const app = express();
app.use(express.json());
app.use(express.static(path.join(__dirname, 'public')));

const server = http.createServer(app);
const wss = new WebSocketServer({ server });

// ── 状态 ─────────────────────────────────────────────────
const tasks = new Map();
const clients = new Set();
const queue = [];
let runningCount = 0;

// ── 工具函数 ─────────────────────────────────────────────
function genId() {
  return crypto.randomBytes(8).toString('hex');
}

function broadcast(msg) {
  const data = JSON.stringify(msg);
  for (const c of clients) {
    if (c.readyState === 1) c.send(data);
  }
}

function stripAnsi(str) {
  return str.replace(/\x1B\[[0-9;]*[a-zA-Z]/g, '').replace(/\x1B\].*?\x07/g, '');
}

function serializeTask(task) {
  return {
    id: task.id,
    name: task.name,
    type: task.type,
    projectPath: task.projectPath,
    status: task.status,
    phase: task.phase,
    phaseName: task.phaseName,
    createdAt: task.createdAt,
    startedAt: task.startedAt,
    completedAt: task.completedAt,
    error: task.error,
    logCount: task.logs.length,
    results: task.results,
    duration: task.startedAt
      ? (task.completedAt || Date.now()) - task.startedAt
      : 0,
  };
}

// ── WebSocket 处理 ───────────────────────────────────────
wss.on('connection', (ws) => {
  clients.add(ws);

  // 发送当前所有任务
  ws.send(JSON.stringify({
    type: 'init',
    data: { tasks: Array.from(tasks.values()).map(serializeTask) },
  }));

  ws.on('message', (raw) => {
    try {
      const msg = JSON.parse(raw);
      handleMessage(ws, msg);
    } catch {
      ws.send(JSON.stringify({ type: 'error', data: { message: 'Invalid message' } }));
    }
  });

  ws.on('close', () => clients.delete(ws));
});

function handleMessage(ws, msg) {
  switch (msg.type) {
    case 'create_task':
      createTask(msg.data);
      break;
    case 'cancel_task':
      cancelTask(msg.data.taskId);
      break;
    case 'delete_task':
      deleteTask(msg.data.taskId);
      break;
    case 'get_logs': {
      const task = tasks.get(msg.data.taskId);
      if (task) {
        ws.send(JSON.stringify({
          type: 'task_logs',
          data: { taskId: task.id, logs: task.logs },
        }));
      }
      break;
    }
  }
}

// ── 任务创建 ─────────────────────────────────────────────
function createTask(data) {
  const id = genId();
  const taskDir = path.join(CONFIG.tasksDir, id);
  fs.mkdirSync(taskDir, { recursive: true });

  const planFile = path.join(taskDir, 'plan.md');
  fs.writeFileSync(planFile, data.plan || data.prompt || '');

  const projectPath = data.projectPath || process.cwd();
  fs.mkdirSync(projectPath, { recursive: true });
  fs.mkdirSync(path.join(projectPath, 'docs'), { recursive: true });

  const task = {
    id,
    name: data.name || '未命名任务',
    type: data.type || 'full',
    projectPath,
    plan: data.plan || '',
    planFile,
    taskDir,
    status: 'queued',
    phase: 0,
    phaseName: '等待中',
    createdAt: Date.now(),
    startedAt: null,
    completedAt: null,
    logs: [],
    error: null,
    currentProcess: null,
    results: null,
    prompt: data.prompt || '',
  };

  tasks.set(id, task);
  broadcast({ type: 'task_created', data: serializeTask(task) });

  queue.push(id);
  tryExecuteNext();
}

// ── 任务执行引擎 ─────────────────────────────────────────
function tryExecuteNext() {
  while (runningCount < CONFIG.maxConcurrent && queue.length > 0) {
    const id = queue.shift();
    const task = tasks.get(id);
    if (task && task.status === 'queued') {
      runningCount++;
      executeTask(task).finally(() => {
        runningCount--;
        tryExecuteNext();
      });
    }
  }
}

async function executeTask(task) {
  task.status = 'running';
  task.startedAt = Date.now();
  broadcast({ type: 'task_updated', data: serializeTask(task) });

  try {
    if (task.type === 'quick') {
      task.phase = 1;
      task.phaseName = '执行中';
      broadcast({ type: 'task_updated', data: serializeTask(task) });
      await runClaude(task, task.prompt || task.plan, 200);
    } else {
      const phases = [
        { name: '生成开发文档', turns: CONFIG.phaseMaxTurns[0], prompt: buildPlanningPrompt(task) },
        { name: '自主开发', turns: CONFIG.phaseMaxTurns[1], prompt: buildDevPrompt(task) },
        { name: '生成报告', turns: CONFIG.phaseMaxTurns[2], prompt: buildReportPrompt(task) },
      ];

      for (let i = 0; i < phases.length; i++) {
        if (task.status === 'cancelled') break;

        task.phase = i + 1;
        task.phaseName = phases[i].name;
        broadcast({ type: 'task_updated', data: serializeTask(task) });

        appendLog(task, `\n═══════════════════════════════════════\n  阶段 ${i + 1}/3: ${phases[i].name}\n═══════════════════════════════════════\n\n`, 'system');

        await runClaude(task, phases[i].prompt, phases[i].turns);
      }
    }

    if (task.status !== 'cancelled') {
      task.status = 'completed';
      task.results = collectResults(task);
      appendLog(task, '\n✓ 任务完成\n', 'system');
    }
  } catch (err) {
    if (task.status !== 'cancelled') {
      task.status = 'failed';
      task.error = err.message;
      appendLog(task, `\n✗ 任务失败: ${err.message}\n`, 'system');
    }
  }

  task.completedAt = Date.now();
  if (task.currentProcess) {
    try { task.currentProcess.kill(); } catch {}
    task.currentProcess = null;
  }

  broadcast({ type: 'task_updated', data: serializeTask(task) });

  // 保存日志到文件
  try {
    fs.writeFileSync(
      path.join(task.taskDir, 'output.log'),
      task.logs.map(l => l.text).join('')
    );
  } catch {}
}

function runClaude(task, prompt, maxTurns) {
  return new Promise((resolve, reject) => {
    const args = [
      '-p', prompt,
      '--allowedTools', 'Read,Write,Edit,Bash,Glob,Grep,WebFetch',
      '--max-turns', String(maxTurns),
    ];

    let proc;
    try {
      proc = spawn(CONFIG.claudeBin, args, {
        cwd: task.projectPath,
        env: { ...process.env },
        stdio: ['pipe', 'pipe', 'pipe'],
      });
    } catch (err) {
      return reject(new Error(`启动 Claude Code 失败: ${err.message}`));
    }

    task.currentProcess = proc;

    proc.stdout.on('data', (data) => {
      appendLog(task, stripAnsi(data.toString()), 'stdout');
    });

    proc.stderr.on('data', (data) => {
      appendLog(task, stripAnsi(data.toString()), 'stderr');
    });

    proc.on('close', (code) => {
      task.currentProcess = null;
      if (task.status === 'cancelled' || code === 0) {
        resolve();
      } else {
        reject(new Error(`进程退出，代码 ${code}`));
      }
    });

    proc.on('error', (err) => {
      task.currentProcess = null;
      if (err.code === 'ENOENT') {
        reject(new Error('找不到 claude 命令。请确认已安装 Claude Code CLI，或设置 CLAUDE_BIN 环境变量指向其路径。'));
      } else {
        reject(new Error(`进程错误: ${err.message}`));
      }
    });
  });
}

function appendLog(task, text, stream) {
  const entry = { text, stream, time: Date.now() };
  task.logs.push(entry);

  if (task.logs.length > 10000) {
    task.logs = task.logs.slice(-8000);
  }

  broadcast({ type: 'task_log', data: { taskId: task.id, entry } });
}

// ── Prompt 构建器 ────────────────────────────────────────
function buildPlanningPrompt(task) {
  return `你是一个资深全栈开发者和技术架构师。

请先读取开发计划文件：${task.planFile}

然后执行以下任务：
1. 分析需求，确定最佳技术选型
2. 设计清晰的项目目录结构
3. 将项目分解为可执行的开发阶段
4. 为每个阶段定义明确的任务、涉及文件和验收标准
5. 将完整的开发架构文档写入 ${task.projectPath}/docs/development-plan.md

确保文档详细、可执行，后续可以直接按照文档逐步实现。`;
}

function buildDevPrompt(task) {
  return `你是一个资深全栈开发者，正在进行自主开发。

请先读取 ${task.projectPath}/docs/development-plan.md 了解完整计划，然后按以下规则自主执行：

1. 按阶段顺序逐一实现每个功能模块
2. 每完成一个阶段后运行测试确保代码正确
3. 遇到错误或bug立即修复，不要等待确认
4. 需要安装的依赖直接安装
5. 需要创建的目录直接创建
6. 每完成一个阶段，在 ${task.projectPath}/docs/progress.md 追加进度记录

你拥有完全的自主执行权限，不要等待任何人工确认。直接开始执行。`;
}

function buildReportPrompt(task) {
  return `你刚刚完成了一个项目的自主开发。请基于项目代码和进度记录生成最终报告。

进度记录：${task.projectPath}/docs/progress.md
项目目录：${task.projectPath}

请生成以下三份详细报告：

1. **完成情况报告** → ${task.projectPath}/docs/report-completed.md
   - 所有已完成的功能模块及关键实现说明
   - 项目的运行方式和使用说明

2. **遇到问题报告** → ${task.projectPath}/docs/report-issues.md
   - 开发过程中遇到的所有问题
   - 每个问题的解决方案及妥协策略

3. **遗留问题报告** → ${task.projectPath}/docs/report-remaining.md
   - 尚未解决的问题
   - 需要人工介入的部分
   - 建议的后续优化方向

每份报告要详细、具体、可操作。`;
}

// ── 任务控制 ─────────────────────────────────────────────
function cancelTask(id) {
  const task = tasks.get(id);
  if (!task || task.status === 'completed' || task.status === 'failed') return;

  task.status = 'cancelled';
  if (task.currentProcess) {
    try { task.currentProcess.kill('SIGTERM'); } catch {}
    task.currentProcess = null;
  }
  task.completedAt = Date.now();
  appendLog(task, '\n⚠ 任务已取消\n', 'system');
  broadcast({ type: 'task_updated', data: serializeTask(task) });
}

function deleteTask(id) {
  const task = tasks.get(id);
  if (task) {
    cancelTask(id);
    tasks.delete(id);
    broadcast({ type: 'task_deleted', data: { taskId: id } });
  }
}

// ── 结果收集 ─────────────────────────────────────────────
function collectResults(task) {
  const docsDir = path.join(task.projectPath, 'docs');
  const files = [];

  if (fs.existsSync(docsDir)) {
    for (const entry of fs.readdirSync(docsDir)) {
      const fp = path.join(docsDir, entry);
      try {
        const stat = fs.statSync(fp);
        if (stat.isFile()) {
          files.push({ name: entry, path: fp, size: stat.size, modified: stat.mtimeMs });
        }
      } catch {}
    }
  }

  return { files };
}

// ── API 路由 ─────────────────────────────────────────────
app.get('/api/file', (req, res) => {
  const filePath = req.query.path;
  if (!filePath) return res.status(400).json({ error: 'Missing path' });
  try {
    const content = fs.readFileSync(filePath, 'utf-8');
    res.json({ content });
  } catch {
    res.status(404).json({ error: 'File not found' });
  }
});

// ── 启动 ─────────────────────────────────────────────────
server.listen(CONFIG.port, CONFIG.host, () => {
  const interfaces = os.networkInterfaces();
  const ips = [];
  for (const name of Object.keys(interfaces)) {
    for (const iface of interfaces[name]) {
      if (iface.family === 'IPv4' && !iface.internal) {
        ips.push(iface.address);
      }
    }
  }

  console.log('');
  console.log('  ┌──────────────────────────────────────┐');
  console.log('  │         CLAUDE HUB · Ready           │');
  console.log('  ├──────────────────────────────────────┤');
  console.log(`  │  Local:   http://localhost:${CONFIG.port}      │`);
  for (const ip of ips) {
    console.log(`  │  Network: http://${ip}:${CONFIG.port} │`);
  }
  console.log('  └──────────────────────────────────────┘');
  console.log('');
  console.log('  手机访问请使用 Network 地址（同一 WiFi）');
  console.log('  Ctrl+C 停止服务');
  console.log('');
});

process.on('SIGINT', () => {
  console.log('\n正在关闭...');
  for (const [, task] of tasks) {
    if (task.currentProcess) {
      try { task.currentProcess.kill('SIGTERM'); } catch {}
    }
  }
  server.close();
  process.exit(0);
});
```

## 文件三：`public/index.html`

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>Claude Hub</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700&family=DM+Mono:ital,wght@0,300;0,400;0,500;1,400&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg: #07070d;
      --bg-sidebar: #0a0a14;
      --surface: #0f0f1a;
      --surface-hover: #151524;
      --surface-active: #1b1b30;
      --border: #1a1a2e;
      --border-light: #262640;
      --accent: #22d3ee;
      --accent-dim: #0e7490;
      --accent-glow: rgba(34, 211, 238, 0.1);
      --success: #34d399;
      --warning: #fbbf24;
      --error: #f87171;
      --text: #e2e8f0;
      --text-sec: #94a3b8;
      --text-muted: #475569;
      --font-d: 'Syne', sans-serif;
      --font-m: 'DM Mono', monospace;
      --header-h: 54px;
      --sidebar-w: 290px;
    }

    html, body {
      height: 100%;
      font-family: var(--font-m);
      font-size: 13px;
      background: var(--bg);
      color: var(--text);
      -webkit-font-smoothing: antialiased;
      overflow: hidden;
    }

    /* ── Layout ── */
    #app {
      display: grid;
      grid-template-rows: var(--header-h) 1fr;
      grid-template-columns: var(--sidebar-w) 1fr;
      grid-template-areas: "header header" "sidebar main";
      height: 100vh;
    }

    .header { grid-area: header; }
    .sidebar { grid-area: sidebar; }
    .main { grid-area: main; }

    /* ── Header ── */
    .header {
      display: flex; align-items: center; justify-content: space-between;
      padding: 0 20px;
      background: var(--bg-sidebar);
      border-bottom: 1px solid var(--border);
      z-index: 100;
    }
    .header-left { display: flex; align-items: center; gap: 12px; }
    .menu-btn {
      display: none; background: none; border: none;
      color: var(--text); font-size: 20px; cursor: pointer; padding: 4px;
    }
    .logo {
      font-family: var(--font-d); font-weight: 700;
      font-size: 1.05rem; letter-spacing: 0.1em; color: var(--text);
    }
    .logo span { color: var(--accent); }
    .header-right {
      display: flex; align-items: center; gap: 14px;
      font-size: 0.6875rem; color: var(--text-sec);
    }
    .conn-dot {
      width: 7px; height: 7px; border-radius: 50%;
      background: var(--error); transition: all .3s;
    }
    .conn-dot.on {
      background: var(--success);
      box-shadow: 0 0 8px rgba(52,211,153,.5);
    }
    .task-badge {
      padding: 2px 8px; background: var(--surface);
      border: 1px solid var(--border); border-radius: 4px;
      font-size: 0.625rem;
    }

    /* ── Sidebar ── */
    .sidebar {
      background: var(--bg-sidebar);
      border-right: 1px solid var(--border);
      display: flex; flex-direction: column; overflow: hidden;
    }
    .new-btn {
      margin: 14px; padding: 11px;
      background: var(--accent); color: #000; border: none; border-radius: 6px;
      font-family: var(--font-d); font-weight: 600; font-size: 0.85rem;
      cursor: pointer; transition: all .2s; letter-spacing: 0.02em;
    }
    .new-btn:hover { background: #06b6d4; transform: translateY(-1px); }
    .task-list { flex: 1; overflow-y: auto; padding: 0 8px 16px; }
    .task-item {
      padding: 10px 12px; margin-bottom: 2px; border-radius: 6px;
      cursor: pointer; transition: all .12s;
      border-left: 3px solid transparent;
    }
    .task-item:hover { background: var(--surface-hover); }
    .task-item.active { background: var(--surface-active); border-left-color: var(--accent); }
    .task-item-name {
      font-size: 0.8rem; font-weight: 500; margin-bottom: 3px;
      white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
    }
    .task-item-meta {
      display: flex; align-items: center; gap: 6px;
      font-size: 0.625rem; color: var(--text-muted);
    }
    .tdot {
      width: 6px; height: 6px; border-radius: 50%; flex-shrink: 0;
    }
    .tdot.queued { background: var(--text-muted); }
    .tdot.running { background: var(--accent); animation: pulse 1.5s infinite; }
    .tdot.completed { background: var(--success); }
    .tdot.failed { background: var(--error); }
    .tdot.cancelled { background: var(--warning); }
    @keyframes pulse { 0%,100%{opacity:1} 50%{opacity:.3} }

    /* ── Main ── */
    .main {
      display: flex; flex-direction: column; overflow: hidden;
      background: var(--bg);
      background-image:
        linear-gradient(rgba(34,211,238,.015) 1px, transparent 1px),
        linear-gradient(90deg, rgba(34,211,238,.015) 1px, transparent 1px);
      background-size: 48px 48px;
    }

    .empty-state {
      flex: 1; display: flex; flex-direction: column;
      align-items: center; justify-content: center;
      color: var(--text-muted); gap: 10px;
    }
    .empty-icon { font-size: 2.5rem; opacity: .2; }
    .empty-state h2 {
      font-family: var(--font-d); font-size: 1rem;
      font-weight: 600; color: var(--text-sec);
    }
    .empty-state p { font-size: 0.8rem; }

    /* ── Task View ── */
    .task-view { flex: 1; display: flex; flex-direction: column; overflow: hidden; }
    .tinfo {
      padding: 14px 20px; border-bottom: 1px solid var(--border);
      display: flex; align-items: center; gap: 12px; flex-wrap: wrap;
    }
    .tinfo-name { font-family: var(--font-d); font-weight: 600; font-size: 0.95rem; }
    .badge {
      padding: 2px 10px; border-radius: 4px;
      font-size: 0.625rem; font-weight: 500;
    }
    .badge.running { background: rgba(34,211,238,.12); color: var(--accent); }
    .badge.completed { background: rgba(52,211,153,.12); color: var(--success); }
    .badge.failed { background: rgba(248,113,113,.12); color: var(--error); }
    .badge.queued { background: rgba(71,85,105,.15); color: var(--text-muted); }
    .badge.cancelled { background: rgba(251,191,36,.12); color: var(--warning); }
    .tinfo-phase { font-size: 0.6875rem; color: var(--text-sec); }
    .tinfo-dur { font-size: 0.6875rem; color: var(--text-muted); margin-left: auto; }
    .tinfo-acts { display: flex; gap: 6px; }
    .btn-i {
      padding: 5px 10px; background: var(--surface);
      border: 1px solid var(--border); border-radius: 4px;
      color: var(--text-sec); font-size: 0.6875rem; cursor: pointer;
      font-family: var(--font-m); transition: all .12s;
    }
    .btn-i:hover { background: var(--surface-hover); color: var(--text); }
    .btn-i.danger:hover { background: rgba(248,113,113,.1); color: var(--error); border-color: var(--error); }

    /* ── Logs ── */
    .log-wrap { flex: 1; display: flex; flex-direction: column; min-height: 0; }
    .log-bar {
      display: flex; align-items: center; justify-content: space-between;
      padding: 6px 20px; background: var(--surface);
      border-bottom: 1px solid var(--border);
      font-size: 0.6875rem; color: var(--text-sec);
    }
    .log-ctrls { display: flex; gap: 6px; }
    .log-ctrls button {
      padding: 2px 8px; background: none;
      border: 1px solid var(--border); border-radius: 3px;
      color: var(--text-muted); font-size: 0.625rem; cursor: pointer;
      font-family: var(--font-m); transition: all .12s;
    }
    .log-ctrls button:hover { color: var(--text); border-color: var(--border-light); }
    .log-ctrls button.on { color: var(--accent); border-color: var(--accent-dim); }
    .log-viewer {
      flex: 1; overflow-y: auto; padding: 12px 20px;
      background: #040408; font-size: 0.8rem; line-height: 1.7;
    }
    .lline { white-space: pre-wrap; word-break: break-all; }
    .lline.stdout { color: var(--text-sec); }
    .lline.stderr { color: var(--error); }
    .lline.system { color: var(--accent); font-style: italic; }

    /* ── Results ── */
    .results-wrap { flex-shrink: 0; border-top: 1px solid var(--border); }
    .results-bar {
      padding: 6px 20px; background: var(--surface);
      border-bottom: 1px solid var(--border);
      font-size: 0.6875rem; color: var(--text-sec);
      display: flex; align-items: center; justify-content: space-between;
      cursor: pointer; user-select: none;
    }
    .results-bar:hover { background: var(--surface-hover); }
    .results-body { max-height: 220px; overflow-y: auto; padding: 8px 20px; }
    .rfile {
      display: flex; align-items: center; justify-content: space-between;
      padding: 7px 12px; margin-bottom: 3px;
      background: var(--surface); border-radius: 4px;
      border: 1px solid var(--border);
    }
    .rfile-info { display: flex; align-items: center; gap: 8px; }
    .rfile-name { font-size: 0.8rem; }
    .rfile-size { font-size: 0.625rem; color: var(--text-muted); }
    .rfile-btn {
      padding: 2px 10px; background: none;
      border: 1px solid var(--border); border-radius: 3px;
      color: var(--accent); font-size: 0.625rem; cursor: pointer;
      font-family: var(--font-m); transition: all .12s;
    }
    .rfile-btn:hover { background: var(--accent-glow); border-color: var(--accent); }

    /* ── Modals ── */
    .modal-bg {
      position: fixed; inset: 0; background: rgba(0,0,0,.65);
      display: flex; align-items: center; justify-content: center;
      z-index: 200; backdrop-filter: blur(3px);
    }
    .modal {
      background: var(--surface); border: 1px solid var(--border);
      border-radius: 10px; width: 540px; max-width: 92vw;
      max-height: 90vh; overflow-y: auto;
      animation: mIn .2s ease;
    }
    .modal.wide { width: 780px; }
    @keyframes mIn { from{opacity:0;transform:scale(.96) translateY(8px)} to{opacity:1;transform:none} }
    .modal-head {
      display: flex; align-items: center; justify-content: space-between;
      padding: 18px 22px; border-bottom: 1px solid var(--border);
    }
    .modal-head h2 { font-family: var(--font-d); font-size: 0.95rem; font-weight: 600; }
    .modal-head-r { display: flex; gap: 8px; align-items: center; }
    .mx { background: none; border: none; color: var(--text-muted); font-size: 18px; cursor: pointer; padding: 4px; }
    .mx:hover { color: var(--text); }

    /* ── Forms ── */
    form { padding: 22px; }
    .fg { margin-bottom: 14px; }
    .fg label {
      display: block; margin-bottom: 5px;
      font-size: 0.6875rem; color: var(--text-sec); font-weight: 500;
    }
    .fg input, .fg select, .fg textarea {
      width: 100%; padding: 9px 11px;
      background: var(--bg); border: 1px solid var(--border); border-radius: 5px;
      color: var(--text); font-family: var(--font-m); font-size: 0.8rem;
      outline: none; transition: border-color .2s;
    }
    .fg input:focus, .fg select:focus, .fg textarea:focus {
      border-color: var(--accent);
      box-shadow: 0 0 0 2px var(--accent-glow);
    }
    .fg textarea { resize: vertical; min-height: 140px; line-height: 1.6; }
    .fg select { cursor: pointer; }
    .fbtns { display: flex; justify-content: flex-end; gap: 10px; margin-top: 20px; }
    .btn-p {
      padding: 9px 22px; background: var(--accent); color: #000;
      border: none; border-radius: 5px;
      font-family: var(--font-d); font-weight: 600; font-size: 0.85rem;
      cursor: pointer; transition: all .2s;
    }
    .btn-p:hover { background: #06b6d4; }
    .btn-s {
      padding: 9px 22px; background: var(--surface-hover);
      color: var(--text-sec); border: 1px solid var(--border); border-radius: 5px;
      font-family: var(--font-m); font-size: 0.8rem; cursor: pointer;
    }
    .btn-s:hover { background: var(--surface-active); color: var(--text); }

    .file-content {
      padding: 18px 22px; background: #040408;
      color: var(--text-sec); font-family: var(--font-m);
      font-size: 0.8rem; line-height: 1.7;
      overflow: auto; max-height: 60vh;
      white-space: pre-wrap; word-break: break-word;
    }

    /* ── Toast ── */
    .toast-box {
      position: fixed; bottom: 16px; right: 16px; z-index: 300;
      display: flex; flex-direction: column; gap: 6px;
    }
    .toast {
      padding: 10px 16px; background: var(--surface);
      border: 1px solid var(--border); border-radius: 6px;
      font-size: 0.8rem; animation: tIn .3s ease;
    }
    .toast.ok { border-left: 3px solid var(--success); }
    .toast.err { border-left: 3px solid var(--error); }
    .toast.info { border-left: 3px solid var(--accent); }
    @keyframes tIn { from{opacity:0;transform:translateX(16px)} to{opacity:1;transform:none} }

    /* ── Reconnect Banner ── */
    .reconn {
      position: fixed; top: var(--header-h); left: 0; right: 0;
      padding: 6px; background: var(--error); color: #fff;
      text-align: center; font-size: 0.6875rem; z-index: 150;
      animation: slideD .3s ease;
    }
    @keyframes slideD { from{transform:translateY(-100%)} to{transform:none} }

    /* ── Scrollbar ── */
    ::-webkit-scrollbar { width: 5px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb { background: var(--border-light); border-radius: 3px; }
    ::-webkit-scrollbar-thumb:hover { background: var(--text-muted); }

    /* ── Overlay ── */
    .sidebar-ov {
      display: none; position: fixed; inset: 0;
      background: rgba(0,0,0,.5); z-index: 49;
    }

    /* ── Responsive ── */
    @media (max-width: 768px) {
      #app { grid-template-columns: 1fr; grid-template-areas: "header" "main"; }
      .menu-btn { display: block; }
      .sidebar {
        position: fixed; left: -300px; top: var(--header-h); bottom: 0;
        width: var(--sidebar-w); z-index: 50; transition: left .25s ease;
      }
      .sidebar.open { left: 0; }
      .sidebar-ov.open { display: block; }
      .tinfo { flex-direction: column; align-items: flex-start; gap: 8px; }
      .tinfo-dur { margin-left: 0; }
      .tinfo-acts { width: 100%; }
      .tinfo-acts .btn-i { flex: 1; text-align: center; }
    }
  </style>
</head>
<body>
<div id="app">
  <!-- Header -->
  <header class="header">
    <div class="header-left">
      <button class="menu-btn" onclick="toggleSidebar()">&#9776;</button>
      <h1 class="logo">CLAUDE <span>HUB</span></h1>
    </div>
    <div class="header-right">
      <span class="conn-dot" id="connDot"></span>
      <span id="connText">连接中...</span>
      <span class="task-badge" id="taskBadge">0 任务</span>
    </div>
  </header>

  <!-- Sidebar -->
  <aside class="sidebar" id="sidebar">
    <button class="new-btn" onclick="showModal('newModal')">+ 新建任务</button>
    <div class="task-list" id="taskList"></div>
  </aside>
  <div class="sidebar-ov" id="sidebarOv" onclick="toggleSidebar()"></div>

  <!-- Main -->
  <main class="main">
    <div class="empty-state" id="emptyState">
      <div class="empty-icon">&#x2B21;</div>
      <h2>创建或选择一个任务</h2>
      <p>提交你的开发计划，Claude 自主执行，你只看结果</p>
    </div>

    <div class="task-view" id="taskView" style="display:none">
      <div class="tinfo" id="tinfo"></div>
      <div class="log-wrap">
        <div class="log-bar">
          <span>实时日志</span>
          <div class="log-ctrls">
            <button id="autoScrollBtn" class="on" onclick="toggleScroll()">自动滚动</button>
            <button onclick="copyLogs()">复制日志</button>
          </div>
        </div>
        <div class="log-viewer" id="logViewer"></div>
      </div>
      <div class="results-wrap" id="resultsWrap" style="display:none">
        <div class="results-bar" onclick="toggleResults()">
          <span>结果文件</span>
          <span id="resultsToggle">&#9660;</span>
        </div>
        <div class="results-body" id="resultsBody"></div>
      </div>
    </div>
  </main>

  <!-- New Task Modal -->
  <div class="modal-bg" id="newModal" style="display:none" onclick="if(event.target===this)hideModal('newModal')">
    <div class="modal">
      <div class="modal-head">
        <h2>新建任务</h2>
        <button class="mx" onclick="hideModal('newModal')">&#10005;</button>
      </div>
      <form id="taskForm" onsubmit="submitTask(event)">
        <div class="fg">
          <label>任务名称</label>
          <input type="text" id="fName" placeholder="例如：用户管理后台 API" required>
        </div>
        <div class="fg">
          <label>任务类型</label>
          <select id="fType" onchange="onTypeChange()">
            <option value="full">完整开发（规划 &#8594; 执行 &#8594; 报告）</option>
            <option value="quick">快速任务（单次执行）</option>
          </select>
        </div>
        <div class="fg">
          <label>项目路径</label>
          <input type="text" id="fPath" placeholder="/Users/you/projects/my-app" required>
        </div>
        <div class="fg" id="planFg">
          <label>开发计划</label>
          <textarea id="fPlan" placeholder="详细描述你的开发需求...&#10;&#10;例如：&#10;开发一个用户管理后台&#10;- 前端：React + Ant Design&#10;- 后端：Express + Prisma&#10;- 功能：用户CRUD、角色权限、JWT认证&#10;&#10;今天只做后端 API 部分"></textarea>
        </div>
        <div class="fg" id="promptFg" style="display:none">
          <label>执行命令</label>
          <textarea id="fPrompt" rows="6" placeholder="输入要 Claude 执行的命令..."></textarea>
        </div>
        <div class="fbtns">
          <button type="button" class="btn-s" onclick="hideModal('newModal')">取消</button>
          <button type="submit" class="btn-p">创建并执行</button>
        </div>
      </form>
    </div>
  </div>

  <!-- File Viewer Modal -->
  <div class="modal-bg" id="fileModal" style="display:none" onclick="if(event.target===this)hideModal('fileModal')">
    <div class="modal wide">
      <div class="modal-head">
        <h2 id="fileTitle">文件</h2>
        <div class="modal-head-r">
          <button class="btn-i" onclick="copyFile()">复制内容</button>
          <button class="mx" onclick="hideModal('fileModal')">&#10005;</button>
        </div>
      </div>
      <pre class="file-content" id="fileContent"></pre>
    </div>
  </div>
</div>

<!-- Toast container -->
<div class="toast-box" id="toastBox"></div>

<!-- Reconnect banner -->
<div class="reconn" id="reconnBanner" style="display:none">连接断开，正在重连...</div>

<script>
// ══════════════════════════════════════════════════════════
//  State
// ══════════════════════════════════════════════════════════
let ws = null;
let tasks = [];
let selectedId = null;
let autoScroll = true;
let reconnTimer = null;
let curFileContent = '';
let resultsExpanded = true;

// ══════════════════════════════════════════════════════════
//  WebSocket
// ══════════════════════════════════════════════════════════
function connect() {
  const proto = location.protocol === 'https:' ? 'wss:' : 'ws:';
  ws = new WebSocket(`${proto}//${location.host}`);

  ws.onopen = () => {
    setConn(true);
    if (reconnTimer) { clearTimeout(reconnTimer); reconnTimer = null; }
  };

  ws.onclose = () => {
    setConn(false);
    reconnTimer = setTimeout(connect, 3000);
  };

  ws.onerror = () => ws.close();

  ws.onmessage = (e) => {
    try { handleMsg(JSON.parse(e.data)); } catch {}
  };
}

function send(type, data) {
  if (ws && ws.readyState === 1) ws.send(JSON.stringify({ type, data: data || {} }));
}

// ══════════════════════════════════════════════════════════
//  Message Handler
// ══════════════════════════════════════════════════════════
function handleMsg(msg) {
  switch (msg.type) {
    case 'init':
      tasks = msg.data.tasks;
      renderList();
      if (selectedId) selectTask(selectedId);
      break;

    case 'task_created':
      tasks.push(msg.data);
      renderList();
      selectTask(msg.data.id);
      toast('任务已创建', 'ok');
      break;

    case 'task_updated': {
      const i = tasks.findIndex(t => t.id === msg.data.id);
      if (i >= 0) tasks[i] = { ...tasks[i], ...msg.data };
      renderList();
      if (selectedId === msg.data.id) {
        renderInfo();
        renderResults();
      }
      if (msg.data.status === 'completed') {
        toast(`"${msg.data.name}" 已完成`, 'ok');
        notify(msg.data.name, '任务已完成');
      } else if (msg.data.status === 'failed') {
        toast(`"${msg.data.name}" 失败`, 'err');
        notify(msg.data.name, '任务失败: ' + (msg.data.error || ''));
      }
      break;
    }

    case 'task_log':
      if (selectedId === msg.data.taskId) appendLog(msg.data.entry);
      break;

    case 'task_logs':
      if (selectedId === msg.data.taskId) {
        const v = document.getElementById('logViewer');
        v.innerHTML = '';
        for (const e of msg.data.logs) appendLog(e);
      }
      break;

    case 'task_deleted':
      tasks = tasks.filter(t => t.id !== msg.data.taskId);
      if (selectedId === msg.data.taskId) {
        selectedId = null;
        showEmpty();
      }
      renderList();
      break;
  }
}

// ══════════════════════════════════════════════════════════
//  Rendering
// ══════════════════════════════════════════════════════════
function renderList() {
  const el = document.getElementById('taskList');
  const running = tasks.filter(t => t.status === 'running').length;
  document.getElementById('taskBadge').textContent = `${running} 运行 / ${tasks.length} 总计`;

  const sorted = tasks.slice().sort((a, b) => b.createdAt - a.createdAt);
  el.innerHTML = sorted.map(t => `
    <div class="task-item ${t.id === selectedId ? 'active' : ''}" onclick="selectTask('${t.id}')">
      <div class="task-item-name">${esc(t.name)}</div>
      <div class="task-item-meta">
        <span class="tdot ${t.status}"></span>
        <span>${statusText(t.status)}</span>
        ${t.status === 'running' ? `<span>&middot; ${t.phaseName}</span>` : ''}
      </div>
    </div>
  `).join('');
}

function selectTask(id) {
  selectedId = id;
  const task = tasks.find(t => t.id === id);
  if (!task) return;

  document.getElementById('emptyState').style.display = 'none';
  document.getElementById('taskView').style.display = 'flex';
  document.getElementById('logViewer').innerHTML = '';

  renderInfo();
  renderResults();
  send('get_logs', { taskId: id });
  renderList();

  if (window.innerWidth <= 768) toggleSidebar();
}

function renderInfo() {
  const t = tasks.find(t => t.id === selectedId);
  if (!t) return;

  document.getElementById('tinfo').innerHTML = `
    <span class="tinfo-name">${esc(t.name)}</span>
    <span class="badge ${t.status}">${statusText(t.status)}</span>
    ${t.status === 'running' ? `<span class="tinfo-phase">阶段 ${t.phase}/3 &middot; ${t.phaseName}</span>` : ''}
    ${t.status === 'failed' && t.error ? `<span class="tinfo-phase" style="color:var(--error)">${esc(t.error).substring(0, 80)}</span>` : ''}
    <span class="tinfo-dur" id="durEl">${fmtDur(t.duration)}</span>
    <div class="tinfo-acts">
      ${t.status === 'running' || t.status === 'queued' ? `<button class="btn-i danger" onclick="cancelTask('${t.id}')">取消</button>` : ''}
      <button class="btn-i danger" onclick="deleteTask('${t.id}')">删除</button>
    </div>
  `;
}

function renderResults() {
  const t = tasks.find(t => t.id === selectedId);
  const wrap = document.getElementById('resultsWrap');
  const body = document.getElementById('resultsBody');

  if (!t || !t.results || !t.results.files || !t.results.files.length) {
    wrap.style.display = 'none';
    return;
  }

  wrap.style.display = 'block';
  body.innerHTML = t.results.files.map(f => `
    <div class="rfile">
      <div class="rfile-info">
        <span class="rfile-name">${fileIcon(f.name)} ${esc(f.name)}</span>
        <span class="rfile-size">${fmtSize(f.size)}</span>
      </div>
      <button class="rfile-btn" onclick="viewFile('${escAttr(f.path)}')">查看</button>
    </div>
  `).join('');
}

function appendLog(entry) {
  const v = document.getElementById('logViewer');
  const d = document.createElement('div');
  d.className = `lline ${entry.stream}`;
  d.textContent = entry.text;
  v.appendChild(d);
  while (v.children.length > 8000) v.removeChild(v.firstChild);
  if (autoScroll) v.scrollTop = v.scrollHeight;
}

function showEmpty() {
  document.getElementById('emptyState').style.display = 'flex';
  document.getElementById('taskView').style.display = 'none';
}

// ══════════════════════════════════════════════════════════
//  Actions
// ══════════════════════════════════════════════════════════
function showModal(id) {
  document.getElementById(id).style.display = 'flex';
  if (id === 'newModal') {
    const saved = localStorage.getItem('ch_path');
    if (saved) document.getElementById('fPath').value = saved;
    document.getElementById('fName').focus();
  }
}
function hideModal(id) { document.getElementById(id).style.display = 'none'; }

function onTypeChange() {
  const v = document.getElementById('fType').value;
  document.getElementById('planFg').style.display = v === 'full' ? 'block' : 'none';
  document.getElementById('promptFg').style.display = v === 'quick' ? 'block' : 'none';
}

function submitTask(e) {
  e.preventDefault();
  const type = document.getElementById('fType').value;
  const data = {
    name: document.getElementById('fName').value,
    type,
    projectPath: document.getElementById('fPath').value,
    plan: type === 'full' ? document.getElementById('fPlan').value : '',
    prompt: type === 'quick' ? document.getElementById('fPrompt').value : '',
  };
  localStorage.setItem('ch_path', data.projectPath);
  send('create_task', data);
  hideModal('newModal');
  document.getElementById('taskForm').reset();
}

function cancelTask(id) { send('cancel_task', { taskId: id }); toast('正在取消...', 'info'); }
function deleteTask(id) { send('delete_task', { taskId: id }); }

function toggleScroll() {
  autoScroll = !autoScroll;
  const b = document.getElementById('autoScrollBtn');
  b.textContent = autoScroll ? '自动滚动' : '手动滚动';
  b.className = autoScroll ? 'on' : '';
}

function copyLogs() {
  const lines = document.getElementById('logViewer').innerText;
  navigator.clipboard.writeText(lines).then(() => toast('日志已复制', 'ok'));
}

function toggleSidebar() {
  document.getElementById('sidebar').classList.toggle('open');
  document.getElementById('sidebarOv').classList.toggle('open');
}

function toggleResults() {
  resultsExpanded = !resultsExpanded;
  document.getElementById('resultsBody').style.display = resultsExpanded ? 'block' : 'none';
  document.getElementById('resultsToggle').textContent = resultsExpanded ? '\u25BC' : '\u25B6';
}

// ── File Viewer ──
async function viewFile(fpath) {
  try {
    const r = await fetch(`/api/file?path=${encodeURIComponent(fpath)}`);
    const d = await r.json();
    curFileContent = d.content;
    document.getElementById('fileTitle').textContent = fpath.split('/').pop();
    document.getElementById('fileContent').textContent = d.content;
    showModal('fileModal');
  } catch { toast('无法读取文件', 'err'); }
}

function copyFile() {
  navigator.clipboard.writeText(curFileContent).then(() => toast('已复制', 'ok'));
}

// ══════════════════════════════════════════════════════════
//  Utilities
// ══════════════════════════════════════════════════════════
function setConn(on) {
  document.getElementById('connDot').className = `conn-dot ${on ? 'on' : ''}`;
  document.getElementById('connText').textContent = on ? '已连接' : '断开';
  document.getElementById('reconnBanner').style.display = on ? 'none' : 'block';
}

function statusText(s) {
  return { queued: '等待中', running: '运行中', completed: '已完成', failed: '失败', cancelled: '已取消' }[s] || s;
}

function fmtDur(ms) {
  if (!ms) return '';
  const s = Math.floor(ms / 1000), m = Math.floor(s / 60), h = Math.floor(m / 60);
  if (h > 0) return `${h}h ${m % 60}m`;
  if (m > 0) return `${m}m ${s % 60}s`;
  return `${s}s`;
}

function fmtSize(b) {
  if (b < 1024) return b + 'B';
  if (b < 1048576) return (b / 1024).toFixed(1) + 'KB';
  return (b / 1048576).toFixed(1) + 'MB';
}

function esc(s) { const d = document.createElement('div'); d.textContent = s; return d.innerHTML; }
function escAttr(s) { return s.replace(/'/g, "\\'").replace(/\\/g, '\\\\'); }

function fileIcon(name) {
  if (name.includes('completed')) return '\u2705';
  if (name.includes('issues')) return '\u26A0\uFE0F';
  if (name.includes('remaining')) return '\u23F3';
  if (name.includes('plan')) return '\uD83D\uDCDD';
  if (name.includes('progress')) return '\uD83D\uDCCA';
  return '\uD83D\uDCC4';
}

function toast(msg, type) {
  const box = document.getElementById('toastBox');
  const t = document.createElement('div');
  t.className = `toast ${type || 'info'}`;
  t.textContent = msg;
  box.appendChild(t);
  setTimeout(() => t.remove(), 3500);
}

function notify(title, body) {
  if ('Notification' in window && Notification.permission === 'granted') {
    new Notification(`Claude Hub \u00B7 ${title}`, { body });
  }
}

// ── Duration Timer ──
setInterval(() => {
  for (const t of tasks) {
    if (t.status === 'running') t.duration = Date.now() - t.startedAt;
  }
  if (selectedId) {
    const t = tasks.find(t => t.id === selectedId);
    if (t && t.status === 'running') {
      const el = document.getElementById('durEl');
      if (el) el.textContent = fmtDur(t.duration);
    }
  }
}, 1000);

// ── Keyboard Shortcuts ──
document.addEventListener('keydown', (e) => {
  if (e.key === 'Escape') {
    document.querySelectorAll('.modal-bg').forEach(m => m.style.display = 'none');
  }
  if ((e.ctrlKey || e.metaKey) && e.key === 'n') {
    e.preventDefault();
    showModal('newModal');
  }
});

// ── Init ──
connect();
if ('Notification' in window && Notification.permission === 'default') {
  Notification.requestPermission();
}
</script>
</body>
</html>
```

---

## 启动方式

```bash
# 1. 进入项目目录
cd claude-hub

# 2. 安装依赖
npm install

# 3. 启动
npm start
```

启动后终端会显示：

```
  ┌──────────────────────────────────────┐
  │         CLAUDE HUB · Ready           │
  ├──────────────────────────────────────┤
  │  Local:   http://localhost:3800      │
  │  Network: http://192.168.1.x:3800   │
  └──────────────────────────────────────┘
```

---

## 你的日常使用流程

```
早上打开浏览器 → localhost:3800
  ↓
点击「+ 新建任务」
  ↓
填写：名称、项目路径、粘贴今天的开发计划
  ↓
点击「创建并执行」
  ↓
关掉页面，去做别的事
  ↓
随时用手机打开 192.168.x.x:3800 查看进度
  ↓
下班前查看结果文件（三份报告自动生成在项目 docs/ 下）
```

## 关键特性

| 特性 | 说明 |
|------|------|
| **三阶段自主执行** | 规划 → 开发 → 报告，全程无需人工干预 |
| **快速任务** | 简单指令直接执行，不走三阶段流程 |
| **并行执行** | 默认同时跑 2 个任务，环境变量 `MAX_CONCURRENT` 可调 |
| **实时日志** | WebSocket 推送，看到 Claude 的每一步输出 |
| **结果文件** | 自动收集 docs/ 下生成的所有文档，一键查看 |
| **手机访问** | 同一 WiFi 下用手机浏览器访问局域网 IP |
| **浏览器通知** | 任务完成时推送系统通知 |
| **任务控制** | 支持取消运行中的任务、删除已完成的任务 |
| **键盘快捷键** | `Ctrl+N` 新建任务，`Esc` 关闭弹窗 |

## 手机访问配置

确保手机和电脑在同一 WiFi 下，用电脑的局域网 IP 访问：

```bash
# 查看你电脑的 IP
# macOS:
ipconfig getifaddr en0
# Linux:
hostname -I
# Windows:
ipconfig
```

手机浏览器打开 `http://你的IP:3800` 即可。

如果需要外网访问（不在同一网络），可以用 ngrok：

```bash
ngrok http 3800
```

## 自定义配置

通过环境变量调整：

```bash
# 指定 claude CLI 路径
CLAUDE_BIN=/usr/local/bin/claude npm start

# 最大并行任务数
MAX_CONCURRENT=3 npm start

# 端口号
PORT=4000 npm start
```

---

这套方案的核心价值：**你只负责发起意图和验收结果，中间所有的重复确认、bug 修复、依赖安装全部由 Claude 自主处理**。把你的精力从「操作者」释放为「决策者」。